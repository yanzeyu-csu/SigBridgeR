% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/81-MatchArg.R
\name{MatchArg}
\alias{MatchArg}
\title{Argument Matching with Default Fallback}
\usage{
MatchArg(arg, choices, default = choices[1])
}
\arguments{
\item{arg}{The argument to match against choices. Can be \code{NULL} or a character vector.}

\item{choices}{A character vector of valid choices to match against.}

\item{default}{The default value to return if no match is found and \code{arg} is \code{NULL}.
Defaults to the first element of \code{choices}.}

\item{call}{The calling environment to use for error reporting. Defaults to
\code{rlang::caller_env()}.}
}
\value{
Returns the matched choice from the \code{choices} vector. If no match is found
and \code{arg} is \code{NULL}, returns the \code{default} value. If no match is found and
\code{arg} is not \code{NULL}, throws an informative error.
}
\description{
A robust argument matching function that supports exact matching, partial matching,
and provides sensible defaults when no match is found. This is an internal utility
function not intended for direct use by package users.
}
\details{
This function provides a more flexible alternative to \code{base::match.arg()} with
the following matching strategy:
\enumerate{
\item If \code{arg} is \code{NULL}, returns the \code{default} value
\item Attempts exact matching using \code{base::match()}
\item Falls back to partial matching using \code{base::pmatch()}
\item If no match found and \code{default} is not \code{NULL}, returns \code{default}
\item Otherwise, throws an informative error with valid choices
}

The function uses \code{rlang::caller_env()} for accurate error reporting in the
context where the function was called.
}
\examples{
\dontrun{
# Internal usage examples
MatchArg("app", c("apple", "banana", "application"))  # Returns "apple"
MatchArg(NULL, c("red", "green", "blue"))             # Returns "red" (default)
MatchArg("gr", c("red", "green", "blue"))             # Returns "green"

# Would error: MatchArg("invalid", c("valid1", "valid2"))
}

}
\seealso{
\code{\link[base]{match}} for exact matching \\
\code{\link[base]{pmatch}} for partial matching \\
\code{\link[rlang]{caller_env}} for calling environment context
}
\keyword{internal}
