% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-OverviewVisuailze.R
\name{FetchUMAP}
\alias{FetchUMAP}
\title{Generate UMAP Plots for Seurat Object}
\usage{
FetchUAMP(
  seurat_obj,
  group_by = c("celltype"),
  feature = NULL,
  plot_name = NULL,
  plot_show = FALSE,
  point_size = 0.6,
  plot_color = NULL,
  label_size = 10,
  umap_label = TRUE,
  order = c(2, 1),
  feature_max_cutoff = 2,
  feature_min_cutoff = -2,
  feature_plot_raster = FALSE,
  ...
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing UMAP reduction}

\item{group_by}{Metadata column(s) for cluster visualization (vector).
Set to NULL to disable.}

\item{feature}{Feature(s) to plot expression (vector). Set to NULL to disable.}

\item{plot_name}{Base title for cluster plots}

\item{plot_show}{Logical to display combined plot immediately (default: FALSE)}

\item{point_size}{Point size for UMAP (default: 0.6)}

\item{plot_color}{Color palette for clusters (default: Seurat palette)}

\item{label_size}{Label font size (default: 10)}

\item{label}{Logical to show cluster labels (default: TRUE)}

\item{order}{Plotting order for points (default: c(2,1) - Significant first)}

\item{...}{Additional arguments passed to either:
- \code{Seurat::DimPlot()} for cluster plots
- \code{Seurat::FeaturePlot()} for expression plots}

\item{feature_max_cutoff}{Maximum expression cutoff (default: NA)}

\item{feature_min_cutoff}{Minimum expression cutoff (default: NA)}

\item{feature_plot_raster}{Logical to rasterize feature plots (default: NULL)}
}
\value{
A list of ggplot objects containing:
\itemize{
\item For \code{group_by}: UMAP cluster plots
\item For \code{feature}: Feature expression plots
}
}
\description{
Creates customizable UMAP visualizations for single-cell data, supporting both
cluster visualization and gene expression plotting. Handles multiple plotting
parameters through flexible argument passing.
}
\section{Plotting Logic}{

\enumerate{
\item For each column in \code{group_by}, creates a separate DimPlot
\item For each entry in \code{feature}, creates a separate FeaturePlot
\item Combines all plots when \code{plot_show=TRUE} using patchwork
}
}

\examples{
\dontrun{
# Cluster visualization only
plots <- FetchUAMP(
  seurat_obj = pbmc,
  group_by = c("celltype", "cluster"),
  plot_name = "PBMC"
)

# Feature expression with custom parameters
plots <- FetchUAMP(
  seurat_obj = pbmc,
  feature = c("CD3D", "CD8A"),
  feature_max_cutoff = 3,
  feature_min_cutoff = 0,
)

}

}
