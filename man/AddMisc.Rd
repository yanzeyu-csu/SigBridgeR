% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-AddMisc.R
\name{AddMisc}
\alias{AddMisc}
\title{Safely Add Miscellaneous Data to Seurat Object}
\usage{
AddMisc(
  seurat_obj,
  ..., # key = value
  cover = TRUE # overwrite existing data
)
}
\arguments{
\item{seurat_obj}{A Seurat object to modify}

\item{...}{key-value pairs to add to the \verb{@misc} slot.}

\item{cover}{Logical indicating whether to overwrite existing data. If
(default TRUE).}
}
\value{
The modified Seurat object with added \verb{@misc} data. The original object
structure is preserved with no other modifications.
}
\description{
Adds arbitrary data to the \verb{@misc} slot of a Seurat object with automatic key
conflict resolution. If the key already exists, automatically appends a numeric
suffix to ensure unique key naming (e.g., "mykey_1", "mykey_2").
}
\section{Key Generation Rules}{

\enumerate{
\item If \code{key} doesn't exist: uses as-is
\item If \code{key} exists: appends the next available number (e.g., "key_1", "key_2")
\item If numbered keys exist (e.g., "key_2"): increments the highest number
}
}

\examples{
\dontrun{
# Basic usage
seurat_obj <- AddMisc(seurat_obj, "QC_stats" = qc_df)

# Auto-incrementing example
seurat_obj <- AddMisc(seurat_obj, markers = markers1)
seurat_obj <- AddMisc(seurat_obj, markers = markers2, cover=FALSE)
# Stores as "markers" and "markers_1"

}


}
