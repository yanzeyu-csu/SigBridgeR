% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/26-LP_GSL_Screen.R
\name{DoLP_SGL}
\alias{DoLP_SGL}
\title{Perform LP-SGL Screening Analysis}
\usage{
DoLP_SGL(
  matched_bulk,
  sc_data,
  phenotype,
  label_type = "LP_SGL",
  LPSGL_family = c("logit", "cox", "linear"),
  resolution = 0.6,
  alpha = 0.5,
  nfold = 5,
  dge_analysis = list(run = FALSE, logFC_threshold = 1, pval_threshold = 0.05),
  ...
)
}
\arguments{
\item{matched_bulk}{Bulk expression matrix (features × samples)}

\item{sc_data}{Single-cell RNA-seq data (Seurat object)}

\item{phenotype}{Binary phenotype vector for bulk samples}

\item{label_type}{Character specifying phenotype label type (default: "LP_SGL")}

\item{LPSGL_family}{Type of regression model: "\code{logit}" (logistic), "\code{cox}" (Cox),
or "\code{linear}" (linear regression)}

\item{resolution}{Resolution parameter for Leiden clustering (default: \code{0.6})}

\item{alpha}{Alpha parameter for SGL balancing L1 and L2 penalties (default: \code{0.5})}

\item{nfold}{Number of folds for cross-validation (default: \code{5})}

\item{dge_analysis}{List controlling differential expression analysis:
\itemize{
\item{run: Whether to run DEG analysis (default: \code{FALSE})}
\item{logFC_threshold: Log fold change threshold (default: \code{1})}
\item{pval_threshold: P-value threshold (default: \code{0.05})}
}}

\item{...}{Additional arguments passed to preprocessing functions, e.g.:
\itemize{
\item{verbose: Whether to print progress messages (default: \code{TRUE})}
\item{seed: Random seed for reproducibility (default: \code{123L})}
}}
}
\value{
A list containing:
\item{scRNA_data}{Seurat object with LP-SGL results integrated}
\item{sgl_fit}{Fitted SGL model object}
\item{cvfit}{Cross-validation results}
\item{dge_res}{Differential expression results if requested (NULL otherwise)}
}
\description{
Identifies phenotype-associated cell subpopulations using Lasso-Penalized
Sparse Group Lasso (LP-SGL) with Leiden community detection. This method
integrates bulk and single-cell RNA-seq data to identify cell subpopulations
associated with phenotypic outcomes.
}
\examples{
\dontrun{
# Example using simulated data
set.seed(123)

# Create simulated data
bulk_data <- matrix(rnorm(1000*50), nrow=1000, ncol=50)
sc_data <- matrix(rnorm(1000*500), nrow=1000, ncol=500)
phenotype <- rep(c(0, 1), each=25)

# Run LP-SGL analysis
results <- DoLP_SGL(
matched_bulk = bulk_data,
sc_data = sc_data,
phenotype = phenotype,
LPSGL_family = "logit",
resolution = 0.6,
dge_analysis = list(run = TRUE, logFC_threshold = 1, pval_threshold = 0.05)
)

# Access results
lpsgl_seurat <- results$scRNA_data
sgl_model <- results$sgl_fit
deg_results <- results$dge_res
}

}
\references{
Li J, Zhang H, Mu B, Zuo H, Zhou K. Identifying phenotype-associated subpopulations through LP_SGL. Briefings in Bioinformatics. 2023 Nov 22;25(1):bbad424.
}
\seealso{
Other screen_method: 
\code{\link{DoDEGAS}()},
\code{\link{DoScissor}()},
\code{\link{DoscAB}()},
\code{\link{DoscPAS}()},
\code{\link{DoscPP}()}
}
\concept{LP_SGL}
\concept{screen_method}
